현재 `ai-factory-helper` 프로젝트 기준으로 **VS Code PowerShell**과 **외부 CMD**에서 각각 가상환경 활성화부터 프로그램 실행까지 순서를 정리하면 다음과 같습니다.

> 전제: 프로젝트 위치가 `C:\Temp\ai-factory-helper`이고 `.venv`가 이미 생성되어 있다고 가정합니다.

## 1. VS Code PowerShell에서 실행

VS Code에서 `ai-factory-helper` 폴더를 연 후 **Terminal → New Terminal**을 실행합니다.

### ① 프로젝트 폴더로 이동

이미 해당 폴더라면 생략합니다.

```powershell
cd C:\Temp\ai-factory-helper
```

### ② 가상환경 활성화

```powershell
.\.venv\Scripts\Activate.ps1
```

정상적으로 활성화되면:

```text
(.venv) PS C:\Temp\ai-factory-helper>
```

처럼 표시됩니다.

### ③ Python 확인

```powershell
python --version
```

그리고 가상환경 Python인지 확인하려면:

```powershell
where.exe python
```

첫 번째 결과가 다음과 같으면 정상입니다.

```text
C:\Temp\ai-factory-helper\.venv\Scripts\python.exe
```

### ④ 필요한 패키지 설치

최초 1회 또는 `requirements.txt`가 변경되었을 때 실행합니다.

```powershell
python -m pip install -r requirements.txt
```

현재 `requirements.txt`는 다음 패키지를 포함하는 것이 좋습니다.

```text
fastapi
uvicorn
openai
```

### ⑤ OpenAI API Key 설정

현재 PowerShell 세션에서:

```powershell
$env:OPENAI_API_KEY="본인의_OpenAI_API_Key"
```

모델 설정:

```powershell
$env:OPENAI_MODEL="gpt-5-mini"
```

API Key는 GitHub에 올리거나 화면 캡처로 공유하지 마세요.

### ⑥ AI Factory Helper 실행

```powershell
uvicorn api.index:app --reload
```

정상이라면:

```text
Uvicorn running on http://127.0.0.1:8000
Application startup complete.
```

이 나옵니다.

브라우저에서 `http://127.0.0.1:8000`으로 접속하면 **AI Factory Helper**가 실행됩니다.

종료할 때는:

```text
Ctrl + C
```

가상환경까지 종료하려면:

```powershell
deactivate
```

---

# 2. 외부 CMD에서 실행

Windows 검색창에서 **cmd**를 검색하여 명령 프롬프트를 실행합니다.

### ① 프로젝트 폴더 이동

```cmd
cd /d C:\Temp\ai-factory-helper
```

### ② 가상환경 활성화

CMD에서는 PowerShell과 명령이 다릅니다.

```cmd
.venv\Scripts\activate.bat
```

정상:

```text
(.venv) C:\Temp\ai-factory-helper>
```

### ③ Python 확인

```cmd
python --version
```

```cmd
where python
```

첫 번째 경로가:

```text
C:\Temp\ai-factory-helper\.venv\Scripts\python.exe
```

이면 정상입니다.

### ④ 패키지 설치

최초 1회만 하면 됩니다.

```cmd
python -m pip install -r requirements.txt
```

### ⑤ OpenAI API Key 설정

CMD에서는 PowerShell과 문법이 다릅니다.

```cmd
set OPENAI_API_KEY=본인의_OpenAI_API_Key
```

모델:

```cmd
set OPENAI_MODEL=gpt-5-mini
```

### ⑥ 프로그램 실행

```cmd
uvicorn api.index:app --reload
```

정상:

```text
Uvicorn running on http://127.0.0.1:8000
Application startup complete.
```

브라우저에서 `http://127.0.0.1:8000`에 접속합니다.

종료:

```text
Ctrl + C
```

가상환경 종료:

```cmd
deactivate
```

---

## 매번 실행할 때 필요한 명령만 요약

패키지 설치와 `.venv` 생성은 매번 할 필요가 없습니다.

**VS Code PowerShell**

```powershell
cd C:\Temp\ai-factory-helper
.\.venv\Scripts\Activate.ps1

$env:OPENAI_API_KEY="본인의_API_Key"
$env:OPENAI_MODEL="gpt-5-mini"

uvicorn api.index:app --reload
```

**외부 CMD**

```cmd
cd /d C:\Temp\ai-factory-helper
.venv\Scripts\activate.bat

set OPENAI_API_KEY=본인의_API_Key
set OPENAI_MODEL=gpt-5-mini

uvicorn api.index:app --reload
```

즉, 둘의 가장 큰 차이는 **가상환경 활성화와 환경변수 설정 명령**입니다.

|작업|VS Code PowerShell|외부 CMD|
|---|---|---|
|폴더 이동|`cd C:\Temp\ai-factory-helper`|`cd /d C:\Temp\ai-factory-helper`|
|가상환경 활성화|`.\.venv\Scripts\Activate.ps1`|`.venv\Scripts\activate.bat`|
|API Key|`$env:OPENAI_API_KEY="..."`|`set OPENAI_API_KEY=...`|
|모델|`$env:OPENAI_MODEL="gpt-5-mini"`|`set OPENAI_MODEL=gpt-5-mini`|
|실행|`uvicorn api.index:app --reload`|동일|
|종료|`Ctrl+C`|동일|
|가상환경 종료|`deactivate`|동일|

**중요:** `set`이나 `$env:`로 설정한 API Key는 해당 터미널을 닫으면 사라집니다. 개발이 안정화되면 다음 단계에서 `.env`를 사용하도록 `api/index.py`를 수정하면 **매번 API Key를 입력하지 않고 `uvicorn api.index:app --reload`만 실행**할 수 있습니다.

---
네. 이미 GitHub 저장소에 `ai-factory-helper`를 한 번 올린 상태에서 **수정한 내용을 업데이트하는 절차**는 간단합니다. GitHub 공식 흐름도 `add → commit → push`입니다. ([GitHub Docs](https://docs.github.com/en/migrations/importing-source-code/using-the-command-line-to-import-source-code/adding-locally-hosted-code-to-github?ref=rob-ferguson&utm_source=chatgpt.com "Adding locally hosted code to GitHub - GitHub Docs"))

프로젝트 폴더에서 다음 순서로 실행하세요.

```powershell
cd C:\Temp\ai-factory-helper

git status

git add .

git status

git commit -m "Update FastAPI backend and AI troubleshooting"

git push origin main
```

각 명령의 의미는 다음과 같습니다.

|순서|Command|의미|
|---|---|---|
|1|`git status`|변경된 파일 확인|
|2|`git add .`|변경 파일을 Commit 대상으로 등록|
|3|`git status`|등록된 파일 재확인|
|4|`git commit -m "..."`|변경 내용을 하나의 버전으로 저장|
|5|`git push origin main`|GitHub의 main 브랜치에 업로드|

`git add .`은 현재 디렉터리 아래의 변경 사항을 staging하고, `git commit`은 이를 버전으로 기록합니다. ([GitHub](https://github.com/git-guides/git-add?utm_source=chatgpt.com "Git Guides - git add · GitHub"))

### 이번 프로젝트에 추천하는 Commit

지금까지 수정한 내용이 FastAPI 구조 변경과 실행환경 수정이므로:

```powershell
git add .
git commit -m "Update FastAPI backend and AI API integration"
git push origin main
```

처럼 하면 좋습니다.

### Push 전에 반드시 확인

이번 프로젝트에는 **OpenAI API Key가 있기 때문에 특히 중요합니다.**

```powershell
git status
```

했을 때 다음 파일이 나타나면 안 됩니다.

```text
.env
.env.local
.venv/
```

`.gitignore`에는 최소한 다음을 넣어두세요.

```gitignore
.env
.env.*
.venv/
__pycache__/
*.pyc
.vercel/
```

GitHub도 비밀번호나 API Key 같은 민감정보를 `add`, `commit`, `push`하지 말 것을 명시하고 있습니다. ([GitHub Docs](https://docs.github.com/en/migrations/importing-source-code/using-the-command-line-to-import-source-code/adding-locally-hosted-code-to-github?ref=rob-ferguson&utm_source=chatgpt.com "Adding locally hosted code to GitHub - GitHub Docs"))

### GitHub 연결 상태도 확인하고 싶다면

```powershell
git remote -v
```

정상이라면 `origin`에 GitHub 저장소 주소가 표시됩니다. ([GitHub Docs](https://docs.github.com/en/migrations/importing-source-code/using-the-command-line-to-import-source-code/adding-locally-hosted-code-to-github?ref=rob-ferguson&utm_source=chatgpt.com "Adding locally hosted code to GitHub - GitHub Docs"))

그리고 현재 브랜치는:

```powershell
git branch
```

확인합니다. 예를 들어:

```text
* main
```

이면 정상입니다.

따라서 앞으로 프로그램을 수정할 때마다 기본적으로 **이 4개만 기억하시면 됩니다.**

```powershell
git status
git add .
git commit -m "수정한 내용 설명"
git push origin main
```

이렇게 하면 **VS Code 수정 → 로컬 Git Commit → GitHub 업데이트**까지 완료됩니다.