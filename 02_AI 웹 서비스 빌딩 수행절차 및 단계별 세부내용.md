
**「과제 2A1-3 내 아이디어를 현실로, AI 웹 서비스 빌딩」**을 기준으로 보면, 이 과제의 핵심은 **“AI 코딩 도구를 활용하여 실제 동작하는 AI 웹 서비스를 만들고 → GitHub에 관리하고 → Vercel에 배포하는 것”**입니다. 최종적으로 다른 사람이 URL로 접속해 AI 기능을 직접 사용할 수 있어야 합니다.

아래 순서로 진행하면 가장 이해하기 쉽습니다.

# 1. 전체 수행 흐름

```text
① 서비스 아이디어 선정
        ↓
② 서비스 기획
        ↓
③ 프로젝트 폴더 및 GitHub 구성
        ↓
④ HTML/CSS/JavaScript 화면 개발
        ↓
⑤ 반응형 웹 적용
        ↓
⑥ AI 기능 화면 개발
        ↓
⑦ Python 백엔드 + AI API 연결
        ↓
⑧ 로컬 테스트 및 오류 수정
        ↓
⑨ GitHub 업로드
        ↓
⑩ Vercel 배포
        ↓
⑪ 실제 URL 동작 테스트
        ↓
⑫ README·기획서·스크린샷 작성
        ↓
⑬ 최종 제출
```

과제에서 요구하는 기술 구조는 **프론트엔드 HTML/CSS/JavaScript + 백엔드 Vercel Serverless Functions(Python)**이며, React/Vue 같은 프레임워크는 사용하지 않는 방식입니다.

---

# 2. 1단계 — AI 웹 서비스 아이디어 결정

처음부터 복잡한 서비스를 만들 필요는 없습니다.

이번 과제에서 가장 중요한 것은 **AI 기능이 실제로 동작하는 웹 서비스를 완성하는 것**입니다.

예를 들어 다음과 같은 서비스가 가능합니다.

|서비스 아이디어|사용자가 입력|AI 결과|
|---|---|---|
|여행 추천 AI|지역, 기간, 취향|여행 일정 추천|
|음식 메뉴 추천|재료, 취향|요리 추천|
|자기소개서 도우미|경력, 지원직무|자기소개서 생성|
|제조업 문제해결 AI|설비·알람 내용|원인 및 조치방법|
|행사 안내문 생성기|행사명, 일시, 대상|안내문 자동 생성|

**과제 수행 난이도를 고려하면 기능을 많이 넣기보다 AI 기능 1개를 확실하게 구현하는 것이 좋습니다.**

---

# 3. 2단계 — 서비스 기획서 작성

코딩 전에 먼저 간단한 기획서를 만듭니다.

과제에서 요구하는 핵심 항목은 다음입니다.

```text
서비스명
서비스 목적
타겟 사용자
페이지 구성
핵심 기능
AI 기능
AI 입력
AI 출력
오류 발생 시 처리
```

예를 들어 **“AI 제조설비 문제해결 도우미”**라면:

```text
서비스명
AI Factory Helper

목적
제조현장 작업자가 설비 문제를 입력하면
AI가 예상 원인과 조치방법을 알려주는 서비스

타겟 사용자
중소 제조기업 생산·설비 작업자

AI 입력
설비명
알람코드
문제 증상

AI 출력
예상 원인
확인 항목
조치 방법
주의사항
```

---

# 4. 3단계 — 화면 3개 이상 설계

과제에서는 **최소 3개 이상의 페이지 또는 섹션**이 필요합니다.

가장 간단하게 구성한다면:

```text
HOME
 ├─ 서비스 소개
 │
AI SERVICE
 ├─ 사용자 입력
 └─ AI 분석 결과
 │
ABOUT
 └─ 서비스 설명
```

꼭 HTML 파일을 3개 만들 필요는 없습니다.

한 페이지에서 메뉴를 클릭하면

```text
HOME
AI SERVICE
ABOUT
```

각 섹션으로 이동하도록 만들어도 됩니다.

---

# 5. 4단계 — VS Code 프로젝트 생성

예를 들어 다음과 같이 프로젝트를 만듭니다.

```text
ai-web-service/

│
├─ index.html
│
├─ css/
│   └─ style.css
│
├─ js/
│   └─ app.js
│
├─ api/
│   └─ ai.py
│
├─ images/
│
├─ requirements.txt
│
├─ README.md
│
└─ .gitignore
```

역할은 다음과 같습니다.

```text
index.html
→ 화면 구조

style.css
→ 디자인

app.js
→ 사용자 입력 및 API 호출

api/ai.py
→ Python 백엔드

requirements.txt
→ Python 패키지

README.md
→ 프로젝트 설명
```

과제에서는 프론트엔드와 `api/` 백엔드 구조가 구분되어 있어야 합니다.

---

# 6. 5단계 — GitHub 저장소 생성

GitHub에 새 Repository를 만듭니다.

예:

```text
ai-factory-helper
```

VS Code에서 연결합니다.

```text
git init

git add .

git commit -m "Initial project setup"

git branch -M main

git remote add origin GitHub주소

git push -u origin main
```

이후 기능을 만들 때마다 커밋합니다.

예:

```text
Initial project setup

Add main page

Add responsive design

Add AI input form

Add AI API function

Add error handling

Update README
```

이렇게 하면 **개발 과정을 GitHub 커밋 이력으로 증빙**할 수 있습니다.

---

# 7. 6단계 — 프론트엔드 개발

이제 AI 코딩 도구를 이용해서 HTML/CSS/JavaScript를 개발합니다.

구조는 대략 다음과 같습니다.

```text
사용자
 ↓
index.html
 ↓
입력창
 ↓
JavaScript
 ↓
AI API 호출
 ↓
결과 표시
```

예를 들어:

```text
[설비명]
CNC-05

[알람코드]
2045

[문제 증상]
가공 중 장비가 정지했습니다.

      [AI 분석하기]

------------------------

AI 분석 결과

예상 원인:
...

확인 사항:
...

조치 방법:
...
```

---

# 8. 7단계 — 반응형 웹 적용

PC에서만 잘 보이면 안 됩니다.

과제에서는 모바일에서도 정상적으로 표시되어야 합니다.

CSS에서 보통 다음과 같은 Media Query를 사용합니다.

```css
@media (max-width: 768px) {

}
```

최소한

```text
PC 화면
모바일 화면
```

두 환경에서 확인합니다.

---

# 9. 8단계 — AI 기능 UX 구현

AI 기능에는 반드시 **입력 → 처리 → 출력** 구조가 있어야 합니다.

```text
사용자 입력

       ↓

[AI 분석하기]

       ↓

처리 중...

       ↓

AI 결과
```

그리고 오류 상황도 처리해야 합니다.

과제에서는 다음 중 최소 하나 이상의 실패 처리를 요구합니다.

```text
① 빈 입력

"질문을 입력해주세요."

② API 오류

"AI 서버 연결 중 오류가 발생했습니다."

③ 응답 지연

"응답이 지연되고 있습니다.
잠시 후 다시 시도해주세요."
```

---

# 10. 9단계 — Python 백엔드 개발

핵심 파일은

```text
api/ai.py
```

입니다.

전체 데이터 흐름은 다음과 같습니다.

```text
웹 브라우저
   │
   │ 질문
   ↓
JavaScript
   │
   │ fetch()
   ↓
/api/ai
   │
   ↓
Python
   │
   ↓
AI API
   │
   ↓
Python
   │
   ↓
JSON 응답
   │
   ↓
JavaScript
   │
   ↓
화면 출력
```

즉, 이번 과제에서 반드시 이해해야 할 핵심 구조가 이것입니다.

---

# 11. 10단계 — AI API 연결

예를 들어 AI API를 연결합니다.

중요한 것은 **API Key를 프로그램에 직접 적으면 안 된다는 것**입니다.

```text
잘못된 방식

API_KEY = "실제_API_KEY"
```

대신 환경변수로 관리해야 합니다.

```text
AI_API_KEY
```

그리고 Python에서는 환경변수에서 가져오는 구조로 만듭니다.

과제에서도 API 키를 코드·README·스크린샷 등에 노출하지 말고 환경변수로 관리하도록 명시하고 있습니다.

---

# 12. 11단계 — JavaScript와 Python 연결

`app.js`에서 사용자의 입력을 받아

```text
fetch('/api/ai')
```

방식으로 Python API를 호출합니다.

전체 흐름을 반드시 이해해 두는 것이 좋습니다.

```text
HTML
사용자 입력
 ↓
JavaScript
fetch()
 ↓
Python
/api/ai.py
 ↓
AI API
 ↓
Python
JSON
 ↓
JavaScript
 ↓
HTML
결과 출력
```

과제 목표에도 **사용자 입력 → JavaScript fetch → 백엔드 → 응답 → 화면 반영** 흐름을 설명할 수 있어야 한다고 되어 있습니다.

---

# 13. 12단계 — 로컬 테스트

Vercel에 바로 올리지 말고 먼저 테스트합니다.

확인할 사항은:

```text
□ 메뉴 이동되는가?

□ 3개 이상의 페이지/섹션이 있는가?

□ 모바일 화면이 깨지지 않는가?

□ 사용자 입력이 가능한가?

□ AI API가 호출되는가?

□ AI 결과가 화면에 표시되는가?

□ 빈 입력 오류가 처리되는가?

□ API 오류가 처리되는가?
```

---

# 14. 13단계 — Vercel 배포

이제 GitHub Repository를 Vercel과 연결합니다.

구조는 다음과 같습니다.

```text
VS Code
   ↓
Git Commit
   ↓
GitHub
   ↓
Vercel
   ↓
자동 Build
   ↓
웹 서비스 배포
   ↓
https://xxxxx.vercel.app
```

환경변수도 Vercel에 등록합니다.

```text
AI_API_KEY = ********
```

그리고 실제 배포 URL에서 다시 테스트합니다.

과제에서는 **배포 URL에서 네비게이션·반응형·AI 기능이 실제 동작해야 하며 문제가 있으면 수정 후 재배포**하도록 요구합니다.

---

# 15. 14단계 — README 작성

README에는 최소한 다음 내용을 넣습니다.

```text
# AI Factory Helper

## 1. 서비스 소개

## 2. 주요 기능

## 3. 화면 구성

## 4. AI 기능

## 5. 기술 스택

HTML
CSS
JavaScript
Python
AI API
Vercel

## 6. 프로젝트 구조

## 7. 실행 방법

## 8. 환경변수 설정

## 9. 배포 방법

## 10. 배포 URL
```

---

# 16. 15단계 — 증빙자료 준비

최종 제출 전에 스크린샷을 준비합니다.

필수적으로 준비하면 좋은 것은:

```text
① PC 화면

② 모바일 화면

③ AI 입력 화면

④ AI 결과 출력 화면

⑤ GitHub Repository

⑥ GitHub Commit History

⑦ Vercel 배포 화면

⑧ AI 코딩 도구 사용 과정
```

과제 자체에서는 **데스크톱 + 모바일 + AI 기능 동작 장면**, 그리고 **AI 코딩 도구 사용 대화 로그 또는 스크린샷**을 증빙 자료로 요구합니다.

---

# 17. 최종 제출물 5종

마지막에는 다음 **5개가 모두 있어야 합니다.**

|구분|제출물|핵심 확인사항|
|---|---|---|
|①|배포 웹 서비스|Vercel URL|
|②|GitHub 저장소|전체 소스코드|
|③|README.md|설명·실행·배포·환경변수|
|④|서비스 기획서|목적·타겟·기능·AI I/O|
|⑤|증빙자료|PC/모바일/AI/AI코딩 과정|

이 5종은 과제의 필수 제출 패키지입니다.

---

# 18. 이번 과제에서 가장 중요한 4가지

복잡하게 생각하지 마시고 다음 **4가지만 먼저 성공시키는 것**을 목표로 하면 됩니다.

```text
① HTML/CSS/JS로 웹 화면 만들기

② Python에서 AI API 호출하기

③ JavaScript → Python → AI 연결하기

④ GitHub → Vercel 배포하기
```

결국 기술적으로는 아래 구조 하나를 완성하는 과제라고 볼 수 있습니다.

```text
          사용자
             │
             ▼
     ┌─────────────────┐
     │ HTML / CSS       │
     │ 웹 화면          │
     └────────┬────────┘
              │
           JavaScript
            fetch()
              │
              ▼
     ┌─────────────────┐
     │ Vercel           │
     │ Python /api/ai   │
     └────────┬────────┘
              │
              ▼
         ┌─────────┐
         │ AI API  │
         └────┬────┘
              │
           AI 결과
              │
              ▼
        웹 화면에 출력
```

### 추천 수행 전략

**1차:** 기획 → HTML 화면까지  
**2차:** JavaScript 입력/출력 → Python API  
**3차:** AI API 연결 → 오류 처리  
**4차:** GitHub → Vercel 배포  
**5차:** README → 기획서 → 증빙 → 최종 제출

특히 지금까지 하신 Git/VS Code/GitHub 실습과 바로 연결되는 과제입니다. 다음 단계에서는 **과제용 웹 서비스 주제 하나를 선정한 뒤, 제가 `1단계 기획 → 폴더 생성 → VS Code 코딩 → GitHub → Vercel 배포`까지 실제 실습 순서로 하나씩 진행하는 방식**이 가장 좋겠습니다.